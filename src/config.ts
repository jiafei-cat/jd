import { TASK_ID } from 'enum'

/** 当前系统支持完成的任务列表 */
export const supportTaskId = [
  TASK_ID.VISIT_ARTICLE,
  TASK_ID.STAR_ARTICLE,
  TASK_ID.STAR_PINS,
  TASK_ID.FOLLOW_USER,
  TASK_ID.COLLECT_ARTICLE,
]
